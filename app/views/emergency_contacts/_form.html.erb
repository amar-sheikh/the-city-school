<%= form_with(model: emergency_contact) do |form| %>
  <% if emergency_contact.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(emergency_contact.errors.count, "error") %> prohibited this emergency_contact from being saved:</h2>

      <ul>
        <% emergency_contact.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :guardian_name, style: "display: block" %>
    <%= form.text_field :guardian_name %>
  </div>

  <div>
    <%= form.label :guardian_relation, style: "display: block" %>
    <%= form.text_field :guardian_relation %>
  </div>

  <div>
    <%= form.label :guardian_phone, style: "display: block" %>
    <%= form.text_field :guardian_phone %>
  </div>

  <%= hidden_field_tag :token, params[:token] %>
  <div>
    <%= form.submit %>
  </div>
<% end %>
